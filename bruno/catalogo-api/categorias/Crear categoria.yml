info:
  name: Crear categoria
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/categorias"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
