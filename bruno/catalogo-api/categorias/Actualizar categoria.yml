info:
  name: Actualizar categoria
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{baseUrl}}/categorias/{{categoriaId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
