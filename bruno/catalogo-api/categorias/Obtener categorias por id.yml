info:
  name: Obtener categorias por id
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/categorias/{{categoriaId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
