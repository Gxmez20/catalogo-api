info:
  name: Listar categoria
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/categorias"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
