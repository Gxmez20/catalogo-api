info:
  name: Actualizar producto
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{baseUrl}}/productos/{{productoId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
