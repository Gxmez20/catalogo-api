info:
  name: Eliminar producto
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{baseUrl}}/productos/{{productoId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
