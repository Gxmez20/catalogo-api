info:
  name: Listar todos incluyendo inactivos
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/productos?incluirInactivos=true"
  params:
    - name: incluirInactivos
      value: "true"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
