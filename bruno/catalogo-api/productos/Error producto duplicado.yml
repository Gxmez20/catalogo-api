info:
  name: Error producto duplicado
  type: http
  seq: 9

http:
  method: POST
  url: "{{baseUrl}}/productos"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
