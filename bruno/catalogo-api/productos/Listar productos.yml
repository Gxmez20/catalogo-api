info:
  name: Listar productos
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/productos"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
