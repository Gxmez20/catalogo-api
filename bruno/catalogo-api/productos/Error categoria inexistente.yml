info:
  name: Error categoria inexistente
  type: http
  seq: 8

http:
  method: POST
  url: "{{baseUrl}}/productos"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
