info:
  name: Crear producto
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/productos"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
