info:
  name: Obtener productos por id
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/productos/{{productoId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
